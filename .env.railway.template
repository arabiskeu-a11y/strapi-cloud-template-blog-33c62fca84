# Railway Environment Variables Template
# انسخ هذه المتغيرات إلى Railway Dashboard → Variables

# ═══════════════════════════════════════════════════════════
# 🔧 NODE CONFIGURATION
# ═══════════════════════════════════════════════════════════
NODE_ENV=production
NODE_VERSION=20.18.0
HOST=0.0.0.0
PORT=1337

# ═══════════════════════════════════════════════════════════
# 🔐 STRAPI SECRETS (ولد مفاتيح جديدة باستخدام: node generate-keys.js)
# ═══════════════════════════════════════════════════════════
APP_KEYS=key1,key2,key3,key4
JWT_SECRET=your-jwt-secret-min-32-chars
API_TOKEN_SALT=your-api-token-salt
ADMIN_JWT_SECRET=your-admin-jwt-secret
TRANSFER_TOKEN_SALT=your-transfer-token-salt

# ═══════════════════════════════════════════════════════════
# 🗄️ DATABASE CONFIGURATION (Railway PostgreSQL)
# هذه المتغيرات تُربط تلقائياً من PostgreSQL service
# ═══════════════════════════════════════════════════════════
DATABASE_CLIENT=postgres
DATABASE_URL=${DATABASE_URL}
DATABASE_HOST=${PGHOST}
DATABASE_PORT=${PGPORT}
DATABASE_NAME=${PGDATABASE}
DATABASE_USERNAME=${PGUSER}
DATABASE_PASSWORD=${PGPASSWORD}
DATABASE_SSL=false

# ═══════════════════════════════════════════════════════════
# 📦 IMAGEKIT CONFIGURATION (اختياري - للصور)
# ═══════════════════════════════════════════════════════════
# IMAGEKIT_PUBLIC_KEY=your_public_key
# IMAGEKIT_PRIVATE_KEY=your_private_key
# IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# ═══════════════════════════════════════════════════════════
# 🌐 CORS ORIGINS (أضف رابط Frontend هنا)
# ═══════════════════════════════════════════════════════════
# سيتم تحديثه في config/middlewares.ts
# مثال: https://your-frontend.up.railway.app

# ═══════════════════════════════════════════════════════════
# 📝 ملاحظات:
# ═══════════════════════════════════════════════════════════
# 1. استخدم ${VARIABLE_NAME} للإشارة لمتغيرات من خدمات أخرى
# 2. Railway يربط PostgreSQL تلقائياً
# 3. ولد مفاتيح جديدة باستخدام: node generate-keys.js
# 4. لا تشارك هذا الملف مع أحد (موجود في .gitignore)
